# Inline Code

This paragraph contains `inline code` within the text.

You can use `console.log()` for JavaScript debugging.

In Python, use `print()` function.

The command `pip install md-convert` installs the package.

Multiple inline codes: `a`, `b`, `c` separated by text.
